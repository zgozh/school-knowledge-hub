# 开发公约（AGENTS.md）

> 本文档是给所有参与本项目的 AI 代理（主会话与子代理）的**强制约束**。任何实现者在动工前必须阅读本文并遵守。

## 项目一句话

面向校务管理的 AI 自动数据采集与知识管理中台：自动采集广州大学多源校务信息形成知识库，师生自然语言提问获得附来源的可信答案；双端（管理端+问答端）、三服务分层。

## 全局约束（必读文档）

- **Spec**：`docs/superpowers/specs/2026-08-18-school-knowledge-hub-design.md`（需求与设计唯一真相）
- **ADR**：`docs/adr/ADR-*.md`（技术决策，编号递增，冲突时以 ADR 为准）
- 实现者与评审者必须按 spec + ADR 核验，未落盘的口头约束无效。

## 模型分工与派活（ADR-011）

| 环节 | 模型 | 执行方式 |
|------|------|---------|
| 调度 / 脚手架 / 规划 / 评审 / 集成 | `gpt-5.6-sol` | 主会话直接执行 |
| 后端代码开发（collector/qa_api/model_server/shared） | `gpt-5.6-sol` | 主会话用 `workflow` 按阶段**批量派发**，`agent(prompt, {model: 'gpt-5.6-sol'})` |
| 前端代码开发（frontend） | `gpt-5.6-sol` | 主会话用 `workflow` 按阶段**批量派发**，`agent(prompt, {model: 'gpt-5.6-sol'})` |

**派活规则**：
1. DSH 实测能力边界：`subagent` 工具无法指定模型；`workflow` 可指定模型但每次是新子代理。因此**任务简报必须完全自包含**（不依赖子代理之前的对话记忆）。
2. 每份简报必含：任务目标、涉及文件（精确路径）、接口/契约、验收标准、相关 spec/ADR 条款引用。
3. 按阶段批量派发（一次 workflow 覆盖一个阶段的多个 bite-size 任务），不逐个小任务派发。

## 工程纪律

1. **TDD**：新功能先写失败测试 → 看它失败 → 最小实现 → 看它通过（`test-driven-development`）。
2. **门禁**：spec 未确认不写代码；架构未确认不拆任务；评审不通过不接纳代码。
3. **YAGNI**：spec 第 11 节「范围外」清单内的一律不做。
4. **降级铁律**：可选依赖失败不得拖垮主链路（spec 第 8 节降级链）。
5. **密钥纪律**：`.env` 不进 git；`.env.example` 进 git；绝不硬编码密钥。

## 目录与职责边界

- `collector/` = 知识的生产与治理；`qa_api/` = 知识的消费；`model_server/` = 本地模型推理；`shared/` = 跨服务复用件（客户端单例、配置、基类）。
- 服务间不互相调用业务逻辑，只通过 HTTP API 与共享存储通信。
- 横切关注点（日志/异常/重试）收敛在 `shared/` 基类，业务层只写业务。
